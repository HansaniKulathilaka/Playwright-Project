Download the Playwright Project application and use this command in the terminal to run the application
--bash
npx playwright test

Installation of the dependencies - Node.js and Playwright
--bash
npm init -y
npm install -D @playwright/test
npx playwright install

